# FRED API key — used only at build time by scripts/fetch-data.mjs.
# Get one free at https://fred.stlouisfed.org/docs/api/api_key.html
# In production this lives in Cloudflare Pages build env / GitHub Actions secrets,
# never in the client bundle. Without it, the build falls back to illustrative seed data.
FRED_API_KEY=
